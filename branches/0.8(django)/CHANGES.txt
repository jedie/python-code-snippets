Änderungen von 0.7.x auf 0.8(django):

allgemein:
	Lauffähig nur im URL root:
		Alle Page Links fangen mit / an!
		benutzten von mod_rewrite nötig.
		Evtl. geht auch CGI ohne rewrite und /index.py ???

internes:
	response.page_msg -> request.page_msg


Plugin API:
	Modules/Plugins should only write to self.response

	Methoden:
		alt: self.templates.write("sub_menu", context)
		neu: self._render_template("sub_menu", context)